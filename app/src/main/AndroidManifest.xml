<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.taxivix">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TaxiVIX"
        android:name=".CustomApplication">

        <activity
            android:name=".ui.splashscreen.SplashScreenActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.TaxiVIX">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.confirmaddress.ConfirmAddressActivity" />

        <activity android:name=".ui.confirmaddress.ListCitiesActivity" />

        <activity android:name=".ui.listtaxistands.ListTaxiStandsActivity" />

        <activity android:name=".ui.detailtaxistand.DetailTaxiStandActivity" />
    </application>

</manifest>